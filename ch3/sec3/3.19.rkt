#lang sicp

; redo exercise 3.18 using an algorithm that takes only
; a constant amount of space

; How can we check for a cycle using a constant amount of space?
; - make the call tail recursive
; - no use of auxiliary data structures?

; We do have the fast and slow pointer algorithm for linked lists.
